# pj_price 追加仕様：eBay CSV一括出品（改訂版 2026-09-19）

スマホからの個別出品に時間がかかるため、商品を溜めてeBayのSeller Hub Reports（旧File Exchange）用CSVとして書き出せるようにする。

解消されること：

- 商品説明のHTMLがアプリで使えない問題（CSVの `*Description` 列はHTMLをそのまま受け付ける）
- タイトルをeBayからコピー→計算機に貼付→eBayに戻す往復（計算機が起点になる）
- せどりすとで仕入れ済みの商品を1件ずつ手入力する手間

---

## 0. 実装前に知っておくこと（テンプレート調査の結果）

取得済みテンプレート3本の全列を調べた結果、**前提が1つ崩れている**。

### UPC列・EAN列が存在しない

| カテゴリID | 列数 | 製品識別子の列 |
|---|---|---|
| 139973（Video Games） | 83 | `C:MPN` のみ |
| 261068（Action Figures） | 111 | `C:Brand` / `C:MPN` のみ |
| 261055（Figures & Statues） | 107 | `C:Brand` / `C:MPN` のみ |

`Product:UPC` も `Product:EAN` も3本とも列自体がない。この3カテゴリではeBayが製品識別子を要求していない。

**JANは `C:MPN` に入れる**（13桁をそのまま）。あわせて内部キーとしても保持し、Amazon SP-API・eBay APIを引く鍵と、重複出品の検出キーに使う。

JANが無い行（実データで13行）は `C:MPN` を空欄にする。MPNは必須アスペクトではないため空欄でも投入できる。

### Item Specificsの値はeBayの推奨リストと一致させる必要がある

テンプレートの末尾にはカテゴリごとの推奨値が全部書かれている。ここに載っている文字列と**一字一句同じ**でないと、eBay側で値が認識されず検索フィルタに載らない。

とくに注意：

- Region Codeは `NTSC-J` ではなく **`NTSC-J (Japan)`**
- Platformは `PS4` ではなく **`Sony PlayStation 4`**
- Brandは `Banpresto` ではなく **`BANPRESTO`**（全部大文字）

詳細は「5. Item Specificsの値」に対応表を置く。

---

## 1. 全体の流れ

出品リストへの入口を2つにする。

```
┌─ (A) せどりすとCSVを取り込む ──┐
│                                 ├→ 出品リスト → カテゴリごとにCSV書き出し
└─ (B) 出品文タブから1件追加 ─────┘
```

(B) は既存の流れ（タイトル生成・説明文生成・価格計算）をそのまま使う。
(A) は新規で、まとめて大量に入れる経路。どちらも同じ出品リストに入る。

---

## 2. 出品リスト

### データ構造

localStorageに配列で保持。1件あたり：

| 項目 | (A)せどりすと由来 | (B)手入力由来 |
|---|---|---|
| SKU | せどりすとのSKUをそのまま | 既存の発番ロジック `YYMMDD-端末記号HHMM-仕入値` |
| JAN | CSVのJAN列 | 空（手入力欄を設けてもよい） |
| ASIN | CSVのASIN列 | 空 |
| タイトル（和） | CSVのタイトル列 | — |
| タイトル（英） | 空で取り込み → 3.5節の手順で埋める | 既存のタイトル生成結果 |
| eBayカテゴリID | SKU接頭辞から自動＋手動振り分け | カテゴリ選択 |
| ConditionID | 出品コンディションから自動 | 状態（新品／中古） |
| 説明文HTML | 空で取り込み → 3.5節の手順で埋める | 既存の説明文生成結果 |
| 売値 | 仕入原価・重量から自動計算 | 推奨売値 |
| 最低売値 | 同上 | Best Offer自動拒否ライン |
| 数量 | 個数の合算（後述） | 1 |
| 仕入原価 | CSVの仕入原価 | 仕入値欄 |
| 重量 | CSVの重量＋梱包マージン | 重量欄 |
| FBA連動フラグ | 既定オフ。6.5.1の判定でオン候補を提示 | 既定オフ |
| Amazon粗利・ランク | CSVの `粗利` `ランク` | — |
| 在庫ゼロ時の動作 | 寝かせる / 再調達 / 終了 | 同左 |
| Amazon最安値 | 再調達モードのみ保持 | 同左 |
| eBay ItemID | 初回投入後に紐づけ | 同左 |
| Item Specifics | カテゴリ別に埋める | 各入力欄 |

### 管理UI

- 件数を常に表示（例：「出品リスト 47件」）
- タップで一覧を開き、各行のタイトル・SKU・カテゴリ・売値を表示
- 行ごとに削除できる
- **未完成の行にバッジを出す**（英題が空、カテゴリ未振り分け、説明文が空など）
- 「リストを空にする」ボタン（確認ダイアログ付き）

---

## 3. せどりすとCSVの取り込み

### 運用前提

ユーザーはせどりすと側に**eBay出品専用のリスト**を作る。CSVを書き出したらそのリストを空にして次のリストを作る。つまり取り込むCSVは常に「今回eBayに出す分だけ」であり、取り込み時のフィルタ条件は不要。

### ファイル形式

- 文字コードは **CP932（Shift_JIS）**。UTF-8ではない。取り込み時にデコードする
- 1行目が列ヘッダ、2行目以降がデータ

### 列（全24列）

```
SKU, JAN, ASIN, タイトル, 基準価格, 配送料, 基準コンディション, 基準出品種別,
データ更新日, 参考価格, カテゴリ, ランク, 重量, 粗利, 仕入原価, 個数,
出品予定価格, 出品コンディション, 出品種別, 出品コメント, 仕入日, 仕入店舗,
仕入店舗コード, メモ
```

使うのは **SKU / JAN / ASIN / タイトル / カテゴリ / 重量 / 仕入原価 / 個数 / 出品コンディション** の9列。残りは無視してよい。

### SKUの構造

`カテゴリ-仕入日-状態コード-ASIN-仕入原価`

例：`game-20260714-N-B001ELJWG2-500`

- eBayの `CustomLabel` に**そのまま転用する**（50文字以内に収まる）
- せどりすと側のリストを消しても、SKU末尾から仕入原価を復元できる

### カテゴリの振り分け

SKU接頭辞とカテゴリ列は1対1で対応している。

| 接頭辞 | カテゴリ列 | eBayカテゴリ | 扱い |
|---|---|---|---|
| `game` | TVゲーム | 139973 | 自動 |
| `hobby` | ホビー | 261068 または 261055 | **要手動振り分け** |
| `toy` | おもちゃ | 261068 または 261055 | **要手動振り分け** |
| `dvd` | DVD | — | 取り込み対象外 |
| `cd` | ミュージック | — | 対象外 |
| `book` | 書籍 | — | 対象外 |
| `software` `pc` `electronics` `kitchen` `diy` `musicInst` | 各種 | — | 対象外 |

- 対象外の行は取り込み時に除外し、「◯件を対象外として除外しました」と件数だけ表示する
- `hobby` / `toy` は可動フィギュア（261068）か非可動（261055）かをせどりすとが持っていないため、取り込み後の一覧画面で2択トグルを出して振り分ける。既定は 261055（プライズ品・POP UP PARADE等が多いため）
- DVDは将来対応の余地があるが、リージョンコードと権利の問題があるため今回は入れない

### 重複のまとめ

同一ASINが複数行に分かれていることがある。このまま出すとeBayの重複出品ポリシーに触れる。

**まとめ方**

1. 「ASIN ＋ ConditionID」が同じ行をグループ化する
2. `*Quantity` = グループ内の `個数` の合算
3. 価格計算に使う仕入原価は、グループ内の**最も高い値**を採用する（安い方に合わせると高い個体が赤字になる）
4. SKU（CustomLabel）はグループ内の**先頭行のSKU**を使う
5. まとめた件数を取り込み結果に表示する（例：「24商品・30行をまとめました」）

ConditionIDが違えば別行のまま残す。

### 重量の扱い

- CSVの重量はAmazonの商品重量（グラム、小数）で、**梱包材を含まない**
- 取り込み時に **梱包マージンを加算**する。設定で変更可、既定は `+60g`（緩衝材＋封筒）
- 加算後の値を切り上げて整数グラムにする

### 数量と価格

- 売値・最低売値は、取り込んだ仕入原価と重量を既存のeBay価格計算ロジックに通して自動算出する
- 4地帯の利益を比較して最も薄い地帯（通常は第4地帯＝米国）を基準に推奨売値を決める既存ルールをそのまま使う

---

## 3.5 英題と説明文の埋め方

### 前提の訂正

改訂前の版では、取り込み経路（A）の英題と説明文を「未設定（後述）」とし、フェーズ2のAPI補完で埋まるかのように読める書き方をしていた。**これは誤り。**

- **SP-APIでは英題も説明文も埋まらない。** Catalog Itemsが返すのはbrand・manufacturer・product typeなどの属性で、商品タイトルは日本語。eBay向けの英語80字タイトルにはならない
- 説明文HTMLを返すAPIは存在しない

したがって、取り込んだ行の英題と説明文は**別の手段で埋める必要がある**。これを埋めないかぎり書き出し前チェックで全件弾かれ、CSVは1件も出せない。

### 埋め方（優先順）

**1. 出品文タブの既存ロジックを使う（フェーズ1の本線）**

一覧から行を選ぶと出品文タブに和名・カテゴリ・状態・重量・仕入原価が流し込まれ、既存のタイトル生成・説明文生成をそのまま実行できるようにする。生成結果は元の行に書き戻す。

1件ずつではあるが、**写真のアップロードとeBayとの往復が消える**ため、現行のスマホ出品より大幅に速い。UIは「次の未完成行へ」で連続処理できる形にする。

**2. eBay Browse APIで同一JANの既存出品を引く（フェーズ2）**

他セラーが同じJANで出している場合、その英題を候補として提示する。ゲームは出品されていることが多く、`幻想水滸伝ティアクライス` → `Suikoden Tierkreis` のような**固有名詞の正式英題**が取れる。翻訳では出ない種類の情報なので、ここが本命。

候補はあくまで提示にとどめ、そのまま採用せず編集できるようにする。他セラーのタイトルをそのまま使うと重複や品質のばらつきを持ち込むため。

**3. 機械翻訳（採用しない）**

DeepL等を挟む案はコストがかかるうえ、フィギュア名・ゲームタイトルの正式英題は翻訳では出ない。固有名詞の照合であって翻訳ではないため、2の方法に劣る。

### 運用上の順序

550件を1件ずつ処理するのは現実的でない。**6.5.1のeBay優先判定で件数を絞ってから着手する。**

1. せどりすとCSVを取り込む
2. eBay優先判定で対象を絞る（`eBay利益 > Amazon粗利 × 1.2`、ランクによる緩和あり）
3. 絞った行だけ英題・説明文を埋める
4. CSVを書き出す

一覧の既定の並び順は「eBay優先」かつ「未完成」の行が上に来るようにする。

---

## 4. JANの有無による分岐

JANは13桁。実データでは569/582行が充足しており、欠けは13行程度。

| | JANあり | JANなし |
|---|---|---|
| Item Specificsの自動取得 | SP-API・eBay APIで引ける（フェーズ2） | 引けない |
| 重複出品の検出キー | JAN | ASIN（せどりすと由来）／SKU |
| 一覧での表示 | 通常 | **「JANなし」バッジ**を出す |

JANなしの行も取り込みも出品もできる。ただし自動補完が効かないので、Item Specificsを手で埋める必要がある行として一覧で目立たせる。

### 書き出し済みログ

せどりすと側のリストを消すと、次のバッチで同じ商品を拾っても気づけない。そのため計算機側に記録を残す。

- CSV生成時に **JAN / ASIN / SKU / タイトル / 書き出し日 / 売値 / FBA連動フラグ / 在庫ゼロ時の動作 / Amazon最安値とその取得日 / eBay ItemID** をlocalStorageの別キーに追記する
- FBA連動する商品は、1つの安定SKUに対して個体レコード（仕入日・仕入原価）を複数ぶら下げる構造にする（6.5.2）
- 次回の取り込み時、JANまたはASINが既出の行に **「出品済みの可能性」バッジ**を出す（自動では除外しない。同じ商品を再仕入れすることもあるため）
- ログの一覧表示と、期間を指定しての削除ができるようにする

---

## 5. Item Specificsの値

テンプレート末尾の推奨値リストと一致させること。以下は主要な対応表。

### 139973（Video Games）

| 列 | 値 |
|---|---|
| `*C:Game Name` | 英語のソフト名（タイトルから生成または手入力） |
| `*C:Platform` | 下の対応表 |
| `C:Publisher` | 下の対応表（推奨値リストに載っている表記） |
| `C:Region Code` | `NTSC-J (Japan)` ← 括弧まで含めて固定 |
| `C:Genre` | `Action & Adventure` / `Role Playing` / `Shooter` など推奨値から選択 |
| `C:Release Year` | 西暦4桁 |
| `C:Country of Origin` | `Japan` |
| `C:MPN` | JAN（13桁）。無ければ空欄 |

**Platform対応表**（左：せどりすとのタイトルやSP-APIから得られる表記／右：eBayに入れる文字列）

| 機種 | eBayの値 |
|---|---|
| Switch / Switch2 | `Nintendo Switch` |
| PS5 | `Sony PlayStation 5` |
| PS4 | `Sony PlayStation 4` |
| PS3 | `Sony PlayStation 3` |
| PS2 | `Sony PlayStation 2` |
| PS1 / PS | `Sony PlayStation 1` |
| PS Vita | `Sony PlayStation Vita` |
| PSP | `Sony PSP` |
| 3DS | `Nintendo 3DS` |
| DS | `Nintendo DS` |
| Wii | `Nintendo Wii` |
| Wii U | `Nintendo Wii U` |
| GC / ゲームキューブ | `Nintendo GameCube` |
| GBA | `Nintendo Game Boy Advance` |
| GBC | `Nintendo Game Boy Color` |
| GB | `Nintendo Game Boy` |
| N64 | `Nintendo 64` |
| SFC / スーパーファミコン | `Nintendo Super Famicom` |
| FC / ファミコン | `Nintendo NES` ※要検討 |
| サターン | `Sega Saturn` |
| ドリームキャスト | `Sega Dreamcast` |
| メガドライブ | `Sega Genesis` |

**Publisher対応表**（よく出るもの）

`Konami` / `Capcom` / `Square Enix` / `BANDAI NAMCO Entertainment` / `Nintendo` / `SEGA` / `ATLUS` / コーエーは `KOEI Corporation` / `Nippon Ichi Software` / `Marvelous` / `Compile Heart` / `Idea Factory` / `Aksys Games` / `Spike Chunsoft, Inc.` / `5pb` / `Arc System Works` / `Gust` / `FROM Software`

### 261068 / 261055（フィギュア）

| 列 | 値 |
|---|---|
| `*C:Type` | 261068: `Action Figure` ／ 261055: `Figure`（既定値。セット品は261068で `Set`） |
| `C:Brand` | 下の対応表 |
| `C:Franchise` | 作品名（推奨値リストから選択。例 `Dragon Ball Z` `One Piece` `Demon Slayer: Kimetsu no Yaiba`） |
| `C:Character` | キャラクター名（推奨値リストにあれば合わせる） |
| `C:Material` | `PVC` 固定 |
| `C:Original/Licensed Reproduction` | `Original` 固定 |
| `C:Vintage` | `No` 固定 |
| `C:Signed` | `No` 固定 |
| `C:Country of Origin` | 原産国（既定 `Japan`、中国製は `China`） |
| `C:Model` | 版・バリエーション |
| `C:Series` | **空欄**（後述） |
| `C:MPN` | JAN（13桁）。無ければ空欄 |
| `C:Scale` | 任意。タイトルから自動抽出（後述） |
| `C:Year Manufactured` | 任意。フェーズ2でSP-APIから自動（後述） |
| `C:Item Height` | 任意。既定は空欄（後述） |

**Brand対応表**（推奨値リストの正確な表記）

| メーカー | eBayの値 |
|---|---|
| バンプレスト | `BANPRESTO` |
| グッドスマイルカンパニー | `Good Smile Company` |
| コトブキヤ | `KOTOBUKIYA` |
| 魂ネイションズ | `Tamashii Nations` |
| S.H.フィギュアーツ | `S.H.Figuarts` |
| マックスファクトリー | `Max Factory` |
| メディコム | `MEDICOM` |
| バンダイ | `Bandai` |
| フリュー | `FuRyu` |
| セガ | `SEGA` |
| タイトー | `Taito` |
| ねんどろいど | `Nendoroid` |
| 不明・ノーブランド | `Unbranded` |

### C:Series は空欄にする

推奨値リストを確認したところ、`C:Series` に載っているのは `The Mandalorian` `Transformers` `Beast Wars` `Deep Space Nine` など**欧米IPのシリーズ名ばかり**で、`POP UP PARADE` `figma` `ねんどろいど` といった日本の商品ラインは1つも入っていない。

これらは `C:Brand` 側で表現できる（推奨値に `Nendoroid` `S.H.Figuarts` `Max Factory` `Good Smile Company` がある）。作品名は `C:Franchise` が受け持つ。したがって `C:Series` は空欄でよい。

### 任意入力の3項目（Scale / Year Manufactured / Item Height）

入力欄は置くが、**いずれも必須にしない**。手で埋める機会が限られるため、自動で埋まる分だけ埋めて残りは空欄のまま投入する。

#### C:Scale — 入れる価値がある

推奨値は**コロン区切り**：`1:1 (Life Size)` `1:4` `1:6` `1:8` `1:9` `1:10` `1:12` `1:16` `1:18` `1:20` …

実データのhobby+toy 143件のうち **76件（53%）がタイトルにスケール表記を含む**。タイトルから正規表現で抜いて自動変換できる。

| タイトル中の表記 | 入れる値 |
|---|---|
| `1/8スケール` `1／8` | `1:8` |
| `1/12` | `1:12` |
| `ノンスケール` | **空欄**（eBayに対応する値がない） |

**注意：`1:7` は推奨値リストに無い。** 日本のスケールフィギュアで最も多い1/7が抜けている。投入自体はできる（推奨値は必須の選択肢ではない）が、eBay側の絞り込みフィルタには載らない可能性がある。1件テストで挙動を見て、通らなければ空欄に落とす。

#### C:Year Manufactured — 手入力しない

推奨値は `1900`〜`2026` の西暦のみ。フェーズ2でSP-APIの `release_date` から自動で入る項目なので、**手で埋める運用は想定しない**。フェーズ1では空欄。

#### C:Item Height — 既定は空欄

推奨値は **`5 in` から `50 in` まで、1インチ刻みの整数のみ**。センチもミリも無い。

この仕様のため、日本のフィギュアでは使いにくい：

| 全高 | インチ換算 | eBayに入れられるか |
|---|---|---|
| ねんどろいど 約10cm | 3.9 in | **範囲外**（最小が5 in） |
| figma 約13cm | 5.1 in → `5 in` | 可 |
| POP UP PARADE 約17cm | 6.7 in → `7 in` | 可 |
| 1/7スケール 約24cm | 9.4 in → `9 in` | 可 |

さらに、タイトルに全高が書かれているのは **143件中7件（5%）** だけ。残りはAmazonの商品詳細を開かないと分からない。

SP-APIでも解決しない。Catalog Itemsが返す寸法は `item_package_dimensions`（外箱）であることがほとんどで、**フィギュア本体の全高ではない**。箱は本体より大きいので、そのまま入れると誇大表示になる。

したがって：

- 既定は空欄
- タイトルに `全高約◯◯cm` / `◯◯mm` があれば自動でインチに四捨五入して入れる（5 in未満なら空欄のまま）
- 手入力欄は残すが、大型フィギュアなど入れる価値がある商品だけ使う

### ConditionIDのマッピング

eBay公式の定義（<https://developer.ebay.com/api-docs/sell/static/metadata/condition-id-values.html>）に基づく。

**注意：3000は「一番下の中古」ではない。** 定義は "used previously but is in excellent condition. may have some very minor signs of cosmetic wear" で、中古の**最上位**にあたる。前回の表で `UG`（良い）を5000、`UVG` を4000としていたが、名称と定義に沿って組み直した。

eBay側の中古グレード（US）

| ID | 名称 | 定義の要旨 |
|---|---|---|
| 1000 | New | 未開封・未使用・元の小売パッケージ |
| 1500 | New other / Open box | 未使用だが箱が無い・開封済み。工場二級品を含む |
| 1750 | New with defects | 未使用だが外観に難あり |
| 2750 | Like New | 開封済みだがほとんど使用感なし。傷なし |
| 3000 | Used / Pre-owned | 使用済みだが極めて良好。ごく軽微な外観の使用感のみ |
| 4000 | Very Good | 明らかな傷はないが、わずかな跡や使用感あり。説明書は同梱 |
| 5000 | Good | 擦れ・傷・ひびはあるが穴や裂けはない |
| 6000 | Acceptable | 明らかな使用感。箱や説明書が欠品していることがある |
| 7000 | For parts or not working | 動作しない・部品取り |

**せどりすとからの変換**

| SKU状態コード | 出品コンディション | ConditionID | eBay名称 |
|---|---|---|---|
| `N` | 新品 | 1000 | New |
| `UM` | 新同（ほぼ新品） | 2750 | Like New |
| `UVG` | 非良（非常に良い） | 3000 | Used |
| `UG` | 良い | 4000 | Very Good |
| `UA` | 可 | 5000 | Good |
| `UKN` | 不明 | 5000 | Good |

Amazonの4段階（ほぼ新品／非常に良い／良い／可）に対し、eBayは2750・3000・4000・5000・6000の5段階ある。上の表は**1段ずつ上に寄せず、意味の近いものを当てている**。`UA`（可）を6000ではなく5000にしているのは、Amazonの「可」が eBayの Acceptable ほど悪くないため。6000は破損や欠品がある個体に手動で下げる用。

**カテゴリによって使えるグレードが違う。** 対応表はあくまで既定値で、実際に投入できる値はカテゴリごとに異なる。フィギュア系（261068 / 261055）は細分化されたグレードが無く 1000 / 1500 / 3000 / 7000 だけのことが多い。その場合：

| SKU状態コード | フィギュアでの代替 |
|---|---|
| `N` | 1000 |
| `UM` `UVG` `UG` `UA` `UKN` | 3000 |

正確な対応は **eBay Metadata APIの `getItemConditionPolicies`** でカテゴリIDを指定すれば取得できる。フェーズ2でWorkerから引いて、カテゴリごとの許容値をアプリ側に持たせるのが本筋。フェーズ1では上の既定値で投入し、エラーが出た値だけ手で直す。

### 中古品の画像について

eBayの画像ポリシーは中古品へのストック写真使用を禁じている。**中古の行は実物を撮影する必要がある**。新品はメーカー画像を使えるため、CSV一括出品は新品のほうが圧倒的に向いている。

一覧画面で新品／中古を色分けし、中古行は「要撮影」として区別する。

---

## 6. CSV書き出し

### カテゴリごとに別ファイル

カテゴリによって列構成が違うため、**1ファイルに混在させない**。

- 「CSVを書き出す」ボタンを押すと、リスト内のカテゴリごとにファイルを生成
- ファイル名：`ebay_261068_260919.csv`（カテゴリID＋日付）

### 共通列

| 列 | 値 |
|---|---|
| `*Action(SiteID=US\|Country=US\|Currency=USD\|Version=1193\|CC=UTF-8)` | `Add` |
| `CustomLabel` | SKU |
| `*Category` | カテゴリID |
| `*Title` | 英題（80字以内） |
| `*ConditionID` | 上の対応表 |
| `*Description` | 説明文HTML |
| `*Format` | `FixedPrice` |
| `*Duration` | `GTC` |
| `*StartPrice` | 売値（小数点2桁） |
| `*Quantity` | 数量（まとめた場合は合算値） |
| `*Location` | `Tokyo, Japan` |
| `*DispatchTimeMax` | `3`（設定で変更可） |
| `*ReturnsAcceptedOption` | `ReturnsAccepted` |
| `ShippingProfileName` | `W1000` |
| `ReturnProfileName` | `Returns 30d Buyer Paid` |
| `PaymentProfileName` | `Managed Payments Immediate` |
| `BestOfferEnabled` | `1` |
| `MinimumBestOfferPrice` | 最低売値 |
| `ImmediatePayRequired` | `1` |
| `PicURL` | 画像URL（パイプ区切り、最大24枚。当面は空欄） |

ビジネスポリシー名は設定画面で変更できるようにする（3つとも）。

### ファイル形式

- **文字コードはUTF-8（BOM付き）**
- 1行目に `Info,Version=1.0.0,Template=fx_category_template_EBAY_US`
- 2行目に列ヘッダ（テンプレートと完全一致。列の順序・記号・大文字小文字を変えない）
- 3行目以降にデータ
- 改行はCRLF
- 説明文HTMLにカンマや改行が含まれるため、全フィールドをダブルクオートで囲む
- 値の中のダブルクオートは `""` にエスケープする

### 書き出し前のチェック

次のいずれかに当てはまる行があれば警告を出し、除外するか続行するかを選ばせる。

- 英題が空、または80字を超えている
- 説明文HTMLが空
- カテゴリが未振り分け（hobby / toy）
- 必須Item Specificsが空（ゲーム：Game Name / Platform、フィギュア：Type）
- 売値が最低売値を下回っている
- FBA連動対象で、eBay想定利益がAmazon粗利を下回っている（6.5.1）

---

## 6.5 FBA連動と再出品

### 前提

- せどりすとCSVは `出品種別` が **582行すべてFBA**。このリストは丸ごとFBA納品予定の在庫である
- eBay側の **Out of Stock オプションはオン**。数量0でも出品は終了せず、item ID・ウォッチャー・販売履歴が残る。同一商品のsoldを1つの出品に積み上げていく方針
- したがってこの節の対象は「FBAに納めつつ、eBayでも売れる商品」であり、**出品は使い捨てではなく長く育てる資産**として扱う

### 6.5.1 eBayに回す商品の絞り込み

Amazon側の想定利益はCSVにすでにある（`粗利` 列、全行充足）。eBay側は既存の計算ロジックで出る。両者を比べる。

| 値 | 出どころ |
|---|---|
| Amazon想定利益 | CSVの `粗利`（カンマ区切り文字列。数値に変換する） |
| eBay想定利益 | 既存ロジック。4地帯のうち最も薄い地帯（通常は第4地帯）を基準 |
| 回転の目安 | CSVの `ランク`（Amazonランキング） |

判定：

```
eBay優先 ← eBay想定利益 > Amazon粗利 × (1 + 優先マージン率)
```

- 優先マージン率の既定は **20%**。eBayは梱包・国際発送・問い合わせ対応の手間が重いので、利益が同等ならAmazonに寄せる
- `ランク` が悪い（回転が遅い）行はしきい値を緩める。既定は10万位以下で優先マージン率を0%にする
- 一覧画面で「eBay優先」の行にバッジを出し、並べ替えと一括選択ができるようにする
- しきい値は設定で変更可

`粗利` はせどりすと側のFBA手数料設定と `出品予定価格` に依存する。**設定が古いと比較そのものがずれる**ので、導入時に一度せどりすとの手数料設定を確認すること。

### 6.5.2 CustomLabel（SKU）の扱い

Out of Stockオンで同一出品に積み上げるなら、**CustomLabelは個体ではなく商品に紐づける必要がある**。せどりすとSKU（`game-20260714-N-B001ELJWG2-500`）は仕入日と仕入原価を含むため、2個目を売った時点で実態と合わなくなる。

| 対象 | CustomLabel |
|---|---|
| FBA連動する行 | **ASINベースの安定SKU**（例 `E-B001ELJWG2`）。状態違いは末尾に付す（`E-B001ELJWG2-U`） |
| 一点物（FBA連動しない行） | せどりすとSKUをそのまま |

個体ごとの仕入日・仕入原価は、CSVに載せず**計算機側のログで持つ**。1つの安定SKUに対して複数の個体レコードがぶら下がる形にする。

あわせて、**eBayのItemIDをログに保持する**。数量や価格の更新にはItemIDかCustomLabelが要るため。初回投入後にeBayから取得して紐づける。

### 6.5.3 在庫同期

両チャネルの数量を **FBA在庫数に一致させる**。

```
eBayで売れる → MCFで引き出す → FBA在庫が1減る → 次の同期でeBay数量も1減る
Amazonで売れる → FBA在庫が1減る       → 次の同期でeBay数量も1減る
```

どちらで売れてもFBA在庫が1つ減るだけなので、**二重売りは構造的に起きない**。

**フェーズ1（手動）**

File Exchangeの `*Action=Revise` を使う。更新用CSVを書き出して手動アップロードする。

**重要：Revise CSVにはAdd用のヘッダを流用しない。** File Exchangeは列が含まれていて値が空の場合、その項目を**消す**挙動をすることがある。全列を空欄で出すと、Item Specificsや説明文が飛ぶ。

したがってRevise CSVは**変更する列だけで構成する**。含めない列は「列ごと省く」。

| 列 | 値 |
|---|---|
| `*Action(...)` | `Revise` |
| `ItemID`（あれば）／ `CustomLabel` | 対象の特定 |
| `*Quantity` | 数量を変える場合のみ |
| `*StartPrice` | 価格を変える場合のみ |

数量だけを変えるなら3列、価格も変えるなら4列。それ以上は出さない。

**特定キーについて**

Reviseの特定は **ItemIDが基本**。CustomLabelで引くには、eBay側の設定でSKUによる出品特定が有効になっていて、かつSKUが全出品で一意である必要がある。

初回投入直後はItemIDがまだ手元にないためCustomLabelを使うことになるので、**この前提が成立しているかを先に確認する**（検証手順を参照）。ItemIDを取得できたらログに保存し、以後はItemIDで特定する。

**フェーズ2（自動）**

PWAは開かないと動かないため、定期実行は **Cloudflare Worker の Cron Triggers** に置く。

```
日次 Cron → SP-API getInventorySummaries（FBA在庫数をASIN/SKU単位で取得）
          → ログの数量と比較
          → 差分があれば eBay Inventory API / Trading API ReviseInventoryStatus で更新
          → 更新結果をログに書き戻す
```

### 6.5.4 在庫がゼロになったときの分岐

商品ごとに3択を設定できるようにする。

| 設定 | 動作 |
|---|---|
| **寝かせる**（既定） | 数量0のまま。Out of Stockオンなので出品は生き続け、soldも残る。再仕入れしたら数量を戻す |
| **再調達モード** | Amazon最安値ベースで売値を組み直し、数量1で出し続ける |
| **終了** | 出品を終了する |

**再調達モードの中身**

1. SP-API **Product Pricing**（`getItemOffers` / `getCompetitivePricing`）でASINの最安値を取得
2. `再調達原価 = Amazon最安値 + 国内送料 + 梱包資材費`
3. 既存の計算ロジックに再調達原価を通して新しい売値・最低売値を出す
4. **配送ポリシーを `W1000` → `W2000` に差し替える**。売れてから買うので、ハンドリングタイム3営業日では届かない（別セラーのFBAから自宅着2日＋詰め替え）
5. 数量を1に戻す
6. Reviseで売値・最低売値・配送ポリシー・数量をまとめて更新

**フォールバック**

最安値が上がって、再計算した売値が相場バナーの水準を超える（＝その値段では売れない）場合は、**価格を上げて出すのではなく数量0に落とす**。「寝かせる」に自動で切り替え、一覧に理由を表示する。

### 6.5.5 表示

FBA連動対象の行は、詳細表示で利益を2本並べる。

| 表示 | 計算に使う原価 | 意味 |
|---|---|---|
| 実現利益 | せどりすとの仕入原価 | この個体を売ったときに実際に残る額 |
| 再調達利益 | Amazon最安値＋国内送料＋梱包資材費 | 仕入れ直して続けた場合に残る額 |

Amazon最安値は取得日も保持し、既定30日を過ぎたら「価格が古い」バッジを出す。

### 6.5.6 運用上のリスク

- **真贋リスク**。再調達モードで別セラーから買った商品を自分のアカウントで国際発送することになる。新品かつ評価の高い出品者に限る、という運用ルールが要る。中古は再調達モードの対象外にする
- **MCFの箱**。Amazonの箱・テープのままeBayの荷物として出せない。詰め替えが必ず入る
- **最安値の変動**。取得から発送までの間に上がることがある。フォールバックを必ず効かせる
- **販売制限解除直後のアカウント**。在庫切れによるキャンセルはdefectになる。ゼロ同期が遅れないよう、フェーズ2の自動化を早めに入れるのが望ましい

---

## 7. API連携（フェーズ2）

### 実装上の制約

SP-APIもeBay APIも、秘密鍵とCORSの都合で **GitHub Pagesの静的ページから直接は叩けない**。既存の `proxy/worker.js`（Cloudflare Worker）に中継エンドポイントを追加する。鍵はWorkerのシークレットに置く。

### 何がどこまで埋まるか

| API | 取れるもの | 使いどころ |
|---|---|---|
| **Amazon SP-API** Catalog Items v2022-04-01（ASINまたはJANで照会） | brand / manufacturer / part_number / 重量・寸法 / release_date / browseClassification | ゲームのPlatform・Publisher・Release Year、フィギュアのBrand |
| **eBay Taxonomy API** `getItemAspectsForCategory` | そのカテゴリの必須・推奨アスペクトと**選択肢の一覧** | 列定義の自動更新。テンプレート取り直しの代わりになる |
| **eBay Metadata API** `getItemConditionPolicies` | カテゴリごとに**使えるConditionIDの一覧** | ConditionIDマッピングの自動化 |
| **Amazon SP-API** Product Pricing（`getItemOffers` / `getCompetitivePricing`） | ASINごとの最安値・出品者数 | 再調達モードの原価（6.5.4）。Catalog Itemsとは別API |
| **Amazon SP-API** FBA Inventory（`getInventorySummaries`） | FBA在庫数 | 在庫同期（6.5.3） |
| **eBay Inventory API / Trading API** `ReviseInventoryStatus` | 数量・価格の更新 | 在庫同期の書き込み側 |

定期実行は **Cloudflare Worker の Cron Triggers** に置く。PWAは開かないと動かないため、同期処理をアプリ側に持たせてはいけない。
| **eBay Browse API** `search`（gtin指定） | 同じJANで出ている他セラーの出品 | **英題の候補取得（3.5節）**、相場の把握、Item Specificsの参考 |

期待値の目安：

- **ゲーム（139973）は必須列が全部埋まる**。とくにPlatformは、タイトルからの抽出では8割（193/240）しか取れなかったものが、SP-APIの `browseClassification` で残りも埋まる
- **フィギュアは半分程度**。`C:Brand` はSP-APIのbrandから取れるが、`C:Character` `C:Franchise` `C:Scale` はAmazon JPの商品属性に入っていないことがほとんど。タイトルからの抽出か手入力になる
- **英題と説明文はSP-APIでは埋まらない**（3.5節）。英題はeBay Browse API、説明文は出品文タブの既存ロジックが担当する

したがって **Scale / Year Manufactured / Item Height を入力欄として追加するかの判断は、フィギュアについてだけ必要**。ゲームは入力欄ゼロで回せる。

### フェーズ分け

- **フェーズ1**：API無し。せどりすとCSV → 取り込み → 手で補完 → eBay CSV。これだけで動く
- **フェーズ2**：Worker経由でSP-APIを叩き、取り込み直後に自動補完する
- **フェーズ3**：画像。Cloudflare R2にアップロードして `PicURL` を埋める

---

## 8. 検証手順

1. せどりすとCSVを取り込み、除外件数・まとめ件数が想定どおりか確認
2. 1件だけリストに残して書き出し、Seller Hub → レポート → アップロードから投入
3. **ConditionIDが通るか確認**。とくにフィギュア2カテゴリで 2750 / 4000 / 5000 が使えるかどうか。エラーが出たらメッセージに許容値が出るので、それに合わせて修正する
4. `C:MPN` にJANを入れた行が通るか確認
5. 通ったら3件程度で再テスト（カテゴリごとに1件ずつ、新品と中古を1件ずつ）
6. 問題なければ本番の件数で投入

### Revise の検証（在庫同期を使う前に必ず行う）

7. 投入済みの1件について、**数量だけを変えるRevise CSV**（3列のみ）をアップロードする
8. 反映後、その出品の **Item Specifics・説明文・価格が消えていないか**をeBay上で目視確認する
9. CustomLabelで特定できたかを確認する。できなければeBay側のSKU設定を見直すか、ItemID運用に切り替える
10. ItemIDを控えてログに保存する

### 絞り込みロジックの検証

11. `粗利` 列（`"3,625"` のようにカンマ入りの文字列）が数値に変換されているか確認する
12. eBay優先判定（`eBay利益 > Amazon粗利 × 1.2`、ランク10万位以下で優先マージン率0%）が、既知の数件で期待どおりの結果になるか確認する

---

## 9. 注意

- SKUの端末記号が未設定のまま運用されている（(B)手入力の経路のみ該当）。設定画面で端末記号を入れてから使うこと
- 1回のアップロードは2,000件まで
- テンプレートのバージョン（`Version=1193`）はeBay側で更新されることがある。エラーが出たらテンプレートを取り直して列ヘッダを差し替える
- sw.jsのキャッシュバージョンを上げること

---

## 10. 未確定事項

- フィギュア2カテゴリで細分化グレード（2750 / 4000 / 5000）が使えるか（検証手順3で確定）
- `C:Scale` に `1:7` を入れて通るか（推奨値リストに無い）
- ファミコン（FC）のPlatform値。推奨値リストに `Nintendo NES` `Nintendo Famicom Disk System` `Nintendo Super Famicom` はあるが、カートリッジ版ファミコンに相当する値がない

---

## 変更しないもの

eBayタブの価格計算ロジック、Shopeeタブ、出品文タブの既存機能（タイトル生成・説明文生成・SKU発番・自店の出品を検索）。
